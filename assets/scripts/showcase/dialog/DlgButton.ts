import DialogBase from "../../common/cmpt/base/DialogBase";

const { ccclass, property } = cc._decorator;

@ccclass
export default class DlgButton extends DialogBase {
    public static pUrl: string = 'DlgButton';

    
}
